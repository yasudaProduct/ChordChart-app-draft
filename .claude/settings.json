{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && echo \"$f\" | grep -qE '\\.(ts|tsx|js|jsx|json|yaml|yml|md)$' && pnpm exec prettier --write \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Prettierフォーマット中..."
          }
        ]
      }
    ]
  }
}
